[theme]
base = "dark"

# Primary accent — used for buttons, active elements, links
primaryColor = "#18B6F2"

# Main application background
backgroundColor = "#081522"

# Sidebar, widgets and secondary surfaces
secondaryBackgroundColor = "#0D2233"

# Primary text
textColor = "#EAF2F7"

# Clean modern font
font = "sans serif"
